# Trailing Slash Normalization Test

URLs with and without trailing slashes should be normalized.

## Path Variations

The API documentation [1] is the same as [1].
Root paths [2] and [2] are different (root preserved).
<!-- mdcitefix:refs -->
[1]: https://example.com/api/
[2]: https://example.com/
